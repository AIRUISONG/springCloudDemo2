server:
  port: 8088
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka/
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true